# Language setting
language: node_js

# Version number
node_js:
  - 0.12
  - 0.11

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE
    - M2=two

build:
  #pre_ci:
   # - docker build -t=shippabledocker/sample_node .
  ci:
    - ps -eax
    - ls -al

  
